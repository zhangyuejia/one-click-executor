# 日志错误关键词（判断是否命令是否成功执行）
errorLogWords: ["abort", "error", "fail", "unable", "Could not"]
skipKeyWords: [".java", ".js", ".css", ".html", ".yml", ".xml"]
# 启用id
currentRefId: "wms3.0_dev"
# 异步拉取模块的代码
enablePullAsync: false
# 模块参数
modulesProperties: [
  {
    refId: "wms3.0_dev",
    # 远程仓库名称
    remoteRepoName: "upstream",
    # 命令执行根目录，此处可以为空，为空会被替换为 one-click-executor.dir
    dir: D:\Projects\wms\wms3.0\dev3,
    # 是否需要切换到本地分支
    enableCheckoutLocalBranch: true,
    # 检出本地分支指定的远程仓库
    checkoutRemoteRepoName: "origin",
    # 模块参数
    modulesParams: [
        {
            name: "wms-cloud",
            # 本地分支
            localBranch: "dev",
            # 远程分支
            remoteBranch: [ "dev" ]
        },
        {
            name: "wms-web",
            # 本地分支
            localBranch: "master",
            # 远程分支
            remoteBranch: [ "master" ]
        },
        {
            name: "wms-mobile",
            # 本地分支
            localBranch: "dev",
            # 远程分支
            remoteBranch: [ "dev" ]
        }
    ]
  },
  {
    refId: "wms3.0_test",
    # 远程仓库名称
    remoteRepoName: "upstream",
    # 命令执行根目录，此处可以为空，为空会被替换为 one-click-executor.dir
    dir: D:\Projects\wms\wms3.0\test3,
    # 是否需要切换到本地分支
    enableCheckoutLocalBranch: true,
    # 检出本地分支指定的远程仓库
    checkoutRemoteRepoName: "origin",
    # 模块参数
    modulesParams: [
      {
        name: "wms-cloud",
        # 本地分支
        localBranch: "qas",
        # 远程分支
        remoteBranch: [ "qas" ]
      },
      {
        name: "wms-web",
        # 本地分支
        localBranch: "master",
        # 远程分支
        remoteBranch: [ "master" ]
      },
      {
        name: "wms-mobile",
        # 本地分支
        localBranch: "test",
        # 远程分支
        remoteBranch: [ "test" ]
      }
    ]
  },
  {
    refId: "wms3.0_prod",
    # 远程仓库名称
    remoteRepoName: "upstream",
    # 命令执行根目录，此处可以为空，为空会被替换为 one-click-executor.dir
    dir: D:\Projects\wms\wms3.0\prod3,
    # 是否需要切换到本地分支
    enableCheckoutLocalBranch: true,
    # 检出本地分支指定的远程仓库
    checkoutRemoteRepoName: "origin",
    # 模块参数
    modulesParams: [
      {
        name: "wms-cloud",
        # 本地分支
        localBranch: "master",
        # 远程分支
        remoteBranch: [ "master" ]
      }
    ]
  }
]



